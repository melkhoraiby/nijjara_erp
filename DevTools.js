// Dev helpers (cache clear, debug)
// Key functions: clearCache(), dumpSheet(sheetName,n), simulateUser(email)
// Not for prod
// Required Scopes: none

function clearCache() {}
function dumpSheet(sheetName, n) {}
function simulateUser(email) {}
